# Keep shell scripts LF and Windows batch files CRLF, on every platform.
*.sh text eol=lf
run.sh text eol=lf
*.bat text eol=crlf
run.bat text eol=crlf

# Treat these as binary so git never rewrites their bytes.
*.woff2 binary
*.png binary
*.svg binary
*.pdf binary
*.ico binary
