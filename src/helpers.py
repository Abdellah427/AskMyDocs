def format_response(response: str) -> str:
    """Format the model's response for display.

    Args:
        response: The raw response from the model.

    Returns:
        The response ready to be shown in the interface.
    """
    return f"{response}"
